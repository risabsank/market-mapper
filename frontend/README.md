# Frontend

This folder is reserved for the research dashboard and the collapsible session chatbot described in `scope.md`.

The exact frontend framework is still an open decision, so the structure is organized by product features instead of framework-specific conventions.

